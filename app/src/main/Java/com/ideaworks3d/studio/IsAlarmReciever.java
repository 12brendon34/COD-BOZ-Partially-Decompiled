package com.ideaworks3d.studio;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
//import com.android.vending.expansion.downloader.DownloaderClientMarshaller;

public class IsAlarmReciever extends BroadcastReceiver {
   public void onReceive(Context var1, Intent var2) {
      /*
      try {
         DownloaderClientMarshaller.startDownloadServiceIfRequired(var1, var2, IsDownloaderService.class);
      } catch (PackageManager.NameNotFoundException var3) {
         var3.printStackTrace();
      }
      */
   }
}
